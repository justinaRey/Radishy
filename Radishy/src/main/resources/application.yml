spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: radishycompany@gmail.com
    password: RadishyDollar$$
    properties: 
        mail: 
            smtp:
                auth: true
                starttls:
                    required: true
                    enable: true
#Configuración MySQL
  datasource:
    url: jdbc:mysql://localhost:3306/Radishy?allowPublicKeyRetrieval=true&useSSL=false&useTimezone=true&serverTimezone=GMT&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
#Configuración Creación de tablas (Hibernate)
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
#Para que thymeleaf no guarde datos en cache
    thymeleaf:
      cache: false